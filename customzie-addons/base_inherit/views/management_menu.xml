<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record model="ir.ui.menu" id="mail.menu_root_discuss">
        <field name="groups_id" eval="[(3, ref('base.group_user'))]"/>
    </record>

    <record model="ir.ui.menu" id="mail.menu_root_discuss">
        <field name="groups_id" eval="[(4, ref('base_inherit.make_invisible'))]"/>
    </record>

    <record model="ir.ui.menu" id="calendar.mail_menu_calendar">
        <field name="groups_id" eval="[(3, ref('base.group_user'))]"/>
    </record>

    <record model="ir.ui.menu" id="calendar.mail_menu_calendar">
        <field name="groups_id" eval="[(4, ref('base_inherit.make_invisible'))]"/>
    </record>

</odoo>
